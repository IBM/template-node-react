apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "starter-kit-chart.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "starter-kit-chart.name" . }}
    helm.sh/chart: {{ include "starter-kit-chart.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "starter-kit-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
